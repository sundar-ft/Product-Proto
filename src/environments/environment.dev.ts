// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  api_url: 'https://seezvn05z3.execute-api.us-east-1.amazonaws.com/dev/dkyc_internal/v10/',
  production: true, 
  url: "https://km-edkyc-krakyc-09-dev.fintuple.in/",
  imageFileSize: 2097152,
  applicationId: '56691624',
  enck_Key: 'czNDcmU3S2V5NG0wOW01Rg==',
  mfProdData:'RCAS'
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
